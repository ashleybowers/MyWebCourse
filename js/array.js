let hobbieArray = ['Watching TV', 'Hanging out', 'Reading'];

function showHobbies (hobbies) {
    for (let i = 0; i < hobbies.length; i++) {
        let hobby = hobbies[i];
        console.log(hobby);
    }
}

showHobbies(hobbieArray);